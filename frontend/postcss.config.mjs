// Tailwind v4 подключается плагином Vite, поэтому postcss-плагины тут не нужны.
// Файл нужен, чтобы сборка не подхватила конфиг из корня репозитория (он от старой версии).
export default { plugins: {} };
